{
  "extends": "./base-tsconfig.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,

    // tsc is only used for type-checking.
    "noEmit": true,

    "lib": ["ES2023"],

    "rootDir": ".."
  },
  "include": ["**/*", "../package.json"]
}
